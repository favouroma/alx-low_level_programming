#include <stdio.h>

/**
 * main - Entry point
 * author: favour
 * Return: Always 0 (Success)
 */
int main(void)
{
	char favvy;

	for (favvy = 'a'; favvy <= 'z'; sammy++)
	{
		if (favvy != 'q' && favvy != 'e')
			putchar(favvy);
	}
	putchar('\n');
	return (0);
}

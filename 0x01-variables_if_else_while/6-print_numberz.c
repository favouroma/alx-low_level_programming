#include <stdio.h>

/**
 * main - Entry point
 * author: favvy
 * Return: Always 0(Success)
 */
int main(void)
{
	int favvy;

	for (favvy = 48; favvy < 58; favvy++)
		putchar(favvy);
	putchar('\n');
	return (0);
}

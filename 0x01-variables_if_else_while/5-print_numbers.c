#include <stdio.h>

/**
 * main - Entry point
 * author: favvy
 * Return: Always 0 (Success/correct)
 */
int main(void)
{
	int favvy;

	for (favvy = 0; favvy < 10; favvy++)
	{
		printf("%d", favvy);
	}
	printf("\n");
	return (0);
}

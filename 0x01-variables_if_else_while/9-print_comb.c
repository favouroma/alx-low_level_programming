#include <stdio.h>

/**
 * main - Entry point
 * autor: favvy
 * Return: Always 0 (Success)
 */
int main(void)
{
	int favvy;
	char john;

	for (favvy = 48; favvy < 58; favvy++)
		putchar(john);
	putchar('\n');
	return (0);
}
